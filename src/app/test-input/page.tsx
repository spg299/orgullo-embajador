// TEMPORARY diagnostic page — no app logic, no client JS, no styling.
// Delete this file (and this folder) once the antivirus/paste test is done.
export default function TestInputPage() {
  return (
    <div>
      <p>Correo:</p>
      <input type="email" />
      <p>Contraseña:</p>
      <input type="password" />
    </div>
  );
}
